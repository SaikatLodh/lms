const COURSE = `course:all`;
const COURSE_BY_INSTRUCTOR = `course:instructor`;
const COURSE_SUGGESTION = `course:suggestion`;
const LECTURE = `lecture`;
const PROGRESS = `progress`;
const REVIEW = `review`;
const SCHEDULE = `schedule`;

module.exports = {
  COURSE,
  COURSE_BY_INSTRUCTOR,
  COURSE_SUGGESTION,
  LECTURE,
  PROGRESS,
  REVIEW,
  SCHEDULE,
};
